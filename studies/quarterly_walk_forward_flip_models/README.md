# Quarterly Walk-Forward Flip Models

The runner is fail-closed. Its default mode builds only 2021-2025 outputs;
`--unlock-2026` is accepted only after the pre-2026 manifest and all gates
validate. Generated datasets, fitted model binaries, and run output remain
untracked; their hashes are recorded in manifests.
