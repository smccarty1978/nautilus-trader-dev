# SPEC: deep_pullback_5s_reacceleration_model

**Generated by Study Factory (Canonical flip_prediction)**  
**Source `study.yaml` SHA-256:** `20eca87767cae4c83649e6c3beb13f6e06f45c6f6682bac42eb3555071baae78`  
**Risk Tier:** `2`  
**Execution Runtime:** `NautilusTrader` (`research_workflow.generic_collector.GenericStudyCollector`)

---

## 1. Executive Summary & Objective

Within an established prevailing 1m regime, after a direction-normalized pullback of at least 1.00 ATR and a completed causal 5s regime flip back into alignment with that prevailing regime, can causal market state, pullback geometry, volume/delta, structural-regime context, and the frozen upstream Model C context identify entries that reach +1.00 ATR favorable before -0.75 ATR adverse within 300 seconds?

- **Instrument:** `NQ` on `XCME`
- **Prevailing Population:** `None` (RTH session)
- **Target Event:** `regime_flip` (None within Nones)
- **Feature Set:** `None` (34 features, hash `10ba5be57345cc5ca482661efe2b6b506b616d36ff16a7f7699abe7308553cb6`)

---

## 2. Chronology & Partitioning

- **Training Years:** `[2021, 2022, 2023]`
- **Development/Validation Years:** `[2024]`
- **Diagnostic Years:** `[]`
- **Prohibited Out-of-Scope Years:** `[2025, 2026]`

---

## 3. Causal Timing & Timestamp Invariant

- **Raw Databento Semantic:** `OPEN_STAMPED` (`ts_event`)
- **NautilusTrader Event Dispatch:** `CLOSE_STAMPED` (`ts_init = ts_event + duration_ns`)
- **Causal Rule:** `FULL_BAR_OHLCV_AVAILABLE_ONLY_AT_INTERVAL_CLOSE`
- **Latest Source Availability:** `latest_source_availability_ts <= observation_ts`

---

## 4. Deliverables Manifest & Acceptance

Authoritative source: `config/deliverables_contract.json`. This section is **rendered
from** that contract, never hand-listed. The contract-checker consumes the JSON, not this
prose, so the two cannot drift and the checker cannot substitute a scope of its own.

Deliverables are partitioned by mode: a mode is only accountable for artifacts it can
actually produce, and an artifact belonging to a mode this study is not authorized to run
is out of scope rather than missing.

**Authorized modes:** `collect`

### Mode `collect`

- `candidates.parquet` -- written to `run_dir/collection` by `backtests/nt_runtime/output_manager.py::persist_collection`
- `observations.parquet` -- written to `run_dir/collection` by `backtests/nt_runtime/output_manager.py::persist_collection`
- `collection_manifest.json` -- written to `run_dir/collection` by `backtests/nt_runtime/output_manager.py::persist_collection`
- `run_manifest.json` -- written to `run_dir` by `backtests/nt_runtime/output_manager.py::OutputManager`
- `status.json` -- written to `run_dir` by `backtests/nt_runtime/output_manager.py::persist_collection`

## Derived Causal Inputs

- `frozen_model_c_score` <- `clean_maturity_flip_model_rolling_productivity` (`artifacts/train_experiment_freeze_repaired.json`, availability=`decision_ts`)

## Required Pre-Freeze Gates

- `population_target_gate` (stage=`pre_fit`, artifact=`artifacts/population_target_gate.json`)
