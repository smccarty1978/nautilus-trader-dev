# Contract Audit — Pass 02

**Study:** `es_wick_imbalance_acceptance_v2`
**Scope:** SPEC/deliverables manifest fidelity, terminal-label reachability, Research
Decision Contract adherence, C4/D/E. Causality (A, B, C1-C3, F, G, H) remains out of
scope — covered independently by `audit/pass_02.md` (auditor `causal-audit-scottm-pass01`,
CLEAR) as of the prior composite; no causal-surface file changed in this delta.
**Mode:** PRE-EXECUTION. No run has occurred; findings judge reachability and contract
consistency in the frozen study assets, not produced artifacts.
**Delta reviewed this pass:** `scripts/check_research_decision_fidelity.py` (commit
`e9e22d7`) and its regression coverage `scripts/tests/test_decision_selection_mode_gate.py`.
All findings from pass 01 unrelated to this delta are carried forward by reference, not
re-derived.

---

## Prior findings adjudicated

| # | Prior finding | Status | Evidence |
|---|---|---|---|
| 1 | WARNING (pass 01): `check_research_decision_fidelity.py` only cross-checked `study.yaml features.selection.mode` against `research_decision.yaml baseline_feature_selection.mode` when the decision declared `"none"`; this study's `"train_only"` declaration was never checked at all. | **FIXED** | `scripts/check_research_decision_fidelity.py` lines 105-132 replace the equality/`"none"`-only branch with a permissiveness-ranking check (`_SELECTION_FREEDOM = {None:0, "none":0, "pre_frozen":0, "train_only":1}`) applied unconditionally to every decision mode. `study_rank > decision_rank` now raises `UNAUTHORIZED_FEATURE_DISCOVERY`; an unrecognised mode on either side fails closed with `UNKNOWN_SELECTION_MODE` (lines 124-132). For this study: `study.yaml features` has no `selection` key (unchanged — confirmed by direct re-read), so `study_sel_mode` defaults to `"none"` (rank 0); `research_decision.yaml baseline_feature_selection.mode: "train_only"` (rank 1). `0 <= 1` → correctly permitted as narrower-than-authorized, not silently un-checked. Re-ran the gate logic against this study's live config and confirmed `studies/es_wick_imbalance_acceptance_v2/artifacts/research_decision_fidelity_report.json` now reads `"status": "PASSED"`, `"findings": []` (regenerated by the fix's own execution, timestamp consistent with the new composite). |
| 2 | NOTE (pass 01): `study.yaml operation.kind: train_evaluate` / `model.family: HistGradientBoostingClassifier` are inert canonical-template boilerplate under a collect-only authorized scope, disclosed only at the `research_decision.yaml` layer and not annotated in `study.yaml`/`SPEC.md`. | **NOT FIXED (deliberately, per coordinator)** | Re-read `study.yaml` — `operation.kind`/`model.family` fields unchanged. Coordinator has stated this is intentional: `operation.kind` drives the mode-partitioned deliverables contract, and editing `study.yaml` would restamp the spec hash. Accepting that rationale; this remains accurate and is recorded again below as a NOTE, not re-raised as a new finding under different framing. |

## New findings this pass

None. Regression coverage (`scripts/tests/test_decision_selection_mode_gate.py`) directly
exercises the narrower-vs-broader distinction, the fail-closed case for unrecognised modes,
and — critically — includes `test_real_studies_still_pass_the_tightened_gate`, which asserts
this exact study (by directory name) returns exit code 0 under the tightened gate. Reading
the test confirms it is not a tautology: it invokes the real CLI via `subprocess.run` against
the study's actual `research_decision.yaml`/`study.yaml` on disk, not a fixture standing in
for them.

## Findings carried forward unchanged from pass 01 (not re-derived; all still PASS / N/A)

- Collect-mode deliverables reachable at declared paths — PASS.
- Terminal label reachability (`LABELED_POSITIVE`/`LABELED_NEGATIVE`/`CENSORED`) — PASS.
- `session_end_censoring: true` implementation — PASS.
- `feature_contract.json` / `study.yaml features.source` / provisional-status disclosure — PASS.
- `research_decision.yaml` chronology / `authorized_dates` fidelity — PASS.
- Warmup (E5) respected without crossing prohibited years — PASS.
- C4 (walk-forward, selection seals, promotion gates) — NOT APPLICABLE (single-arm, no model selection).
- D (train/serve skew) — NOT APPLICABLE (no model trained or served).

None of these touch `check_research_decision_fidelity.py`, so they were not re-verified this
pass per the bounded re-audit protocol.

---

### NOTE (carried forward): inert `operation.kind` / `model.family` boilerplate

Unchanged from pass 01. `study.yaml` still carries `operation.kind: train_evaluate` and
`model.family: HistGradientBoostingClassifier`, inherited from the canonical
`flip_prediction` factory template. `research_decision.yaml`'s `scope_caveats` disclaims
model fitting at the decision-contract layer. No mode currently reachable under this study's
`authorized_modes: ["collect"]` invokes training (`backtests/nt_runtime/strategy_binding.py:145`
only recognizes `collect`/`backtest`). The coordinator has confirmed this is deliberate — changing
`study.yaml` would restamp the spec hash, and `operation.kind` is load-bearing for the
mode-partitioned deliverables contract, not free-floating metadata. No reachable defect;
recorded as disclosure hygiene only, does not count toward blocking/warning.

---

## Referred to lookahead-auditor

None.

---

## Blocking verdict

<!-- AUDIT_SUMMARY_V2_START -->
{
  "verdict": "CLEAR",
  "audit_type": "contract",
  "study": "es_wick_imbalance_acceptance_v2",
  "auditor": "contract-audit-mccarty-2026-08-17-p01",
  "audited_execution_composite_sha256": "a580bc38bcda6b03fd96e63dfe47374314ee59c558eff2763f9fe55ebdc1ce98",
  "blocking": 0,
  "warning": 0,
  "not_verified": 0
}
<!-- AUDIT_SUMMARY_V2_END -->

**CLEAR.** The single pass-01 WARNING — the Research Decision Contract fidelity gate never
cross-checking `baseline_feature_selection.mode: "train_only"` — is fixed at
`scripts/check_research_decision_fidelity.py` (commit `e9e22d7`) with a permissiveness-ranked
check applied to every declared mode, verified directly against this study's live
`research_decision.yaml`/`study.yaml` (regenerated `artifacts/research_decision_fidelity_report.json`
now shows `PASSED`, `findings: []`) and covered by a named regression test for this exact
study. No new findings were introduced. The one remaining NOTE (inert `operation.kind`/
`model.family` boilerplate) is disclosure hygiene, deliberately left as-is per the
coordinator's explanation, and does not block. Nothing in this study's contract surface
blocks execution.
