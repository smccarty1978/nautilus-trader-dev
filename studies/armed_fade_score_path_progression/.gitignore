# Generated event table. Rebuild with:
#   python -m studies.armed_fade_score_path_progression.implementation.build_paths
# The JSON artifacts under results/ are summaries and manifests and ARE committed.
results/*.parquet
