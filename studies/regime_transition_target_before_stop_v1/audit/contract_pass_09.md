<!-- AUDIT_SUMMARY_V2_START -->
{"verdict":"CLEAR","audit_type":"contract","auditor":"contract-checker","critical":0,"warning":0,"not_verified":0,"note":4,"study":"regime_transition_target_before_stop_v1","audited_execution_composite_sha256":"1a6eed85254d052ca84c823a4af5a3d643af83fac57310811720086807077c36"}
<!-- AUDIT_SUMMARY_V2_END -->

# Contract Audit — Pass 09 (SEAL authorization only; NOT fitting)

Adjudicates one single-defect fix to the sealed Phase D modeling driver
(`implementation/phase_d_modeling.py` `load_phase_c_inputs` frame-source resolution) plus its
matching synthetic-fixture update. Carries forward every PASS row from pass 08
(composite `4f45256b`, CLEAR). Only the delta is re-adjudicated here.

## Delta scope verification

Comparing the pass-08 `audit/contract_status.json` `audited_files` map to this pass's
`audit/frozen_execution_manifest.json` `file_sha256_map`, exactly the following study inputs
changed:

| File | pass 08 sha256 | pass 09 sha256 | Assessment |
|---|---|---|---|
| `study:implementation/phase_d_modeling.py` | `03c2e4ba…` | `139a13d3…` | the one authorized code change |
| `study:tests/test_phase_d_modeling_driver.py` | `de2ea3c7…` | `ae902163…` | matching fixture update |
| `study:artifacts/phase0_source_manifest.json` | `4b533719…` | `1fd86b8a…` | generated provenance manifest, regenerated by PREPARE for the new composite (NOTE 2) |

Unchanged and identical across both passes: `compiled_study.json` (`2c6219b4…`), `SPEC.md`
(`8b251df0…`), `research_decision.yaml` (`d5755010…`), `study.yaml` (`7ced4a04…`),
`config/feature_contract.json` (`3607b3dd…`), `config/deliverables_contract.json`
(`f4c9180f…`), `config/population_contract.json`, `config/target_contract.json`,
`config/timestamp_contract.json`, `config/baseline.json`, `config/lineage.json`, dataset
identity, and every `repo:` closure file. This establishes structurally that target
semantics, population, the 13-feature contract, folds, the 36-config grid, selection
priority, chronology, the six cell definitions, and the `study.yaml` model block are
untouched — `compiled_study.json` is the compiled projection of all of them and its bytes
are unchanged.

## Delta code review

`load_phase_c_inputs` (`phase_d_modeling.py:115-152`) now:
- loads all three frames; asserts `len(candidates)==len(observations)==len(targets)`
  (`:123-124`) and `candidates[keys].equals(observations[keys])` on
  `[observation_ts, regime_start_ns, checkpoint_index]` (`:125-126`) **before** any concat;
- reads the 13 features from the **candidate** surface — `missing = [c for c in cols if c
  not in candidates]` → `PHASE_D_FEATURE_COLUMNS_MISSING` (`:134-136`);
- resolves `regime_direction` from the **observation** surface via ordered fallback
  `("regime_direction","direction","prevailing_regime")` → `PHASE_D_DIRECTION_COLUMN_MISSING`
  if none (`:137-139`);
- adds the new `PHASE_D_DIRECTION_COLUMN_AMBIGUOUS` guard: refuses if the direction column
  also appears on candidates (`:140-141`);
- positional `pd.concat([candidates, observations[[direction_col]], targets], axis=1)` with a
  post-concat `columns.duplicated()` check (`:142-146`);
- retains the TRAIN-year guard `PHASE_D_NONTRAIN_YEAR_READ` (`:147-150`) and `_direction`
  mapping `1→LONG / -1→SHORT` (`:77-83, 151`).

The change is confined to this function plus a module docstring/comment. `_resolved_cell`
(binary set `{POSITIVE, NEGATIVE, NEGATIVE_SL}`), `FOLDS` (`:36-39`), `DEFAULT_CONFIGURATIONS`
(`product((3,5,7),(7,15,31),(0.03,0.05),(100,500))` = 36, `:40-47`), the selection sort key
`(-min_roc_auc, -mean_roc_auc, -mean_pr_auc, roc_auc_std, configuration_index)` (`:324`),
`ARMS`/`DIRECTIONS` (6 cells), `TRAIN_YEARS`/`OOS_YEARS`, `AUTHORITATIVE_TARGET_SHA256`
(`21d598a8…`) / `_LOGICAL_SHA256` (`552690f0…`), `_assert_target_authority`, `_fit_one`
routing through `research_workflow.modeling.fit_temporal_fold`, and `run_phase_d` are byte-for
the-purposes-of-this-review identical to the pass-08-approved structure.

## Fixture review

`tests/test_phase_d_modeling_driver.py` `_fixture` (`:43-72`) now writes the 13 `f*` feature
columns + population metadata onto the **candidate** rows (`:53-55`) and `regime_direction`
onto the **observation** rows (`:56-57`) — the real `train_merged_collection` layout. This
fixture would fail against the pre-fix driver (features absent from observations) and passes
against the fixed driver, so it now guards the defect that passes 06-08 missed. Section-9
A-K tests and the `test_contract_pass06_*` closure tests are all present and their
assertions (`load_phase_c_inputs` call sites at `:155-158, 189-192, 210-213, 247`) are
consistent with the new layout. `test_contract_pass06_fit_path_routes_only_through_governed_primitive`
(`:262-266`) still asserts the negative `fit_model` imports against current driver bytes.

## Compliance (delta rows)

| Requirement | Verdict | Code evidence | Test evidence | Smallest remediation |
|---|---|---|---|---|
| Fix confined to `load_phase_c_inputs` frame-source resolution; no touch to target semantics / population / 13-feature contract / folds / 36-grid / selection priority / chronology / 6 cells | PASS | `phase_d_modeling.py:115-152` is the only logic change; `compiled_study.json`, `SPEC.md`, `research_decision.yaml`, `study.yaml`, all `config/*.json` byte-identical to pass 08 (sha comparison above). `research_decision.yaml:38-49` `phase_d_modeling_protocol` unchanged. | `test_section9_D_E_temporal_folds_accepted`, `_F_`, `_G_` still pin `FOLDS` and TRAIN-only years; `test_contract_pass06_report_schema_and_per_fold_log_loss` still pins the 2 deliverables. | — |
| Features read from candidate surface; `regime_direction` from observation surface; row-alignment on identity keys asserted before positional concat | PASS | `phase_d_modeling.py:123-126` length + `keys` equality before concat; `:134-136` features from `candidates`; `:137-139` direction from `observations`; `:142` positional concat. | `test_section9_A_H_I_J_K_full_run` H-branch loads via `load_phase_c_inputs` with the new fixture layout and asserts binary label integrity; `test_section9_B` asserts LONG/SHORT `_direction` partition. | — |
| New `PHASE_D_DIRECTION_COLUMN_AMBIGUOUS` guard fails closed on a direction column present on both surfaces | PASS | `phase_d_modeling.py:140-141`. | Not directly exercised by a dedicated negative test. | Add a fixture placing `regime_direction` on both frames and assert the raise. (non-blocking: guard is fail-closed and code path is trivial) |
| Fixture updated to the real Phase C layout so it now guards this defect | PASS | `test_phase_d_modeling_driver.py:53-57` features→candidates, `regime_direction`→observations. | The fixture is the test scaffold for all Section-9 + pass-06 closure tests; all remain internally consistent. | — |
| Direction semantics (`regime_direction 1 → LONG`) contract-consistent | PASS | `phase_d_modeling.py:77-83` deterministic map; `research_decision.yaml:44` cells `LONG_*/SHORT_*`. Continuation crosstab evidence is a causal claim — see referral. | `test_section9_B_long_short_identity_cannot_collide`. | — |
| Frozen execution composite `1a6eed85…` current | PASS | `audit/frozen_execution_manifest.json` self-declares `1a6eed85…` (generated 2026-09-02T02:45:38Z); `audit/preflight.json` `EXECUTION_MANIFEST` = PASSED, `execution_composite_sha256` `1a6eed85…` (run 02:53:45Z, 8/8, `required_checks_missing: []`); `audit/readiness.json` R8 `IDENTITY_STABLE` resolved twice with exact equality → `1a6eed85…`, 148 files, `unresolved_dependencies: []`. | — | Independent CLI re-run of `scripts/resolve_execution_manifest.py` unavailable in this environment (NOTE 3); relied on the preflight re-resolution + R8 double-resolution. |
| Preflight ran every required check and passed | PASS | `audit/preflight.json`: 8 required checks, all `PASSED`, `required_checks_missing: []`, `checks_complete: true`, `status: CLEAR`, same composite. | — | — |
| Readiness passed R1–R10 | PASS | `audit/readiness.json` `overall_status: PASS`; R1 `DATASET_IDENTITY_OK`, R5 `REAL_COLLECTOR_INSTANTIATED`, R8 `IDENTITY_STABLE`, R9 `NO_ALTERNATE_CATALOG_OPENERS` (0 violations), R10 emits exactly the 13 features + 8 metadata cols. | — | — |
| Causal and contract reviews have distinct declared identities | PASS | `audit/pass_ledger.json`: causal pass 08 `lookahead-auditor`, contract pass 08 `contract-checker`; this pass `contract-checker`; `audit/status.json` ≠ `audit/contract_status.json`. Provenance strength `DECLARED_IDENTITY_ONLY` (not authenticated independence). | — | — |
| Section-9 A–K + contract-pass-06 closure tests present and consistent with the new fixture | PASS | `tests/test_phase_d_modeling_driver.py:146-296`. | Present; not executed in this audit (no runner in scope). | — |
| All pass-08 PASS rows still hold (target-authority fail-closed, governed fit via `fit_temporal_fold`, SPEC/deliverables/per-fold `log_loss`, 6 cells + folds + grid + `selection_priority` declared, TRAIN/OOS separation, 2024 unaccessed, no freeze/fit artifacts) | PASS | Driver structure at `:32-33, 101-113, 24/253, 62-65/303-304, 294-350` unchanged; `experiment_authorization.json` TRAIN [2021-23] / OOS [2024] / prohibited [2025-26]; no `train_experiment_freeze.json`, no `artifacts/phase_d/`. `phase0_source_manifest` diff is a generated-artifact refresh, not a contract change. | pass-08 tests carried forward. | — |
| Seal binds report bytes to the audited composite | NOT APPLICABLE (pre-seal) | `artifacts/preexec_audit_seal.json` binds the prior composite `4f45256b…`; the orchestrator re-seals after both pass-09 audits are CLEAR. | — | Orchestrator runs `scripts/preexec_audit_seal.py`. |
| Backtest / fill model / warmup (E) | NOT APPLICABLE | Phase D is TRAIN modeling only; no backtest or entry rule authorized (`research_decision.yaml:38-52`). | — | — |

## Prior-finding adjudication (06–08)

- **Pass 06 (3 CRITICAL) — all remain RESOLVED.** Target authority still authenticated at the
  executable boundary before any output (`_assert_target_authority` at `run_phase_d`, byte
  `21d598a8…` + logical `552690f0…` + reconciliation cross-check); fit path still routes only
  through `research_workflow.modeling.fit_temporal_fold` (`:24, 253`); `deliverables_contract.json`
  (`f4c9180f…`, unchanged) authorizes mode `modeling` → exactly `phase_d_modeling_report.json`
  + `phase_d_model_artifacts.json`, both written (`:354-357`); per-fold `log_loss` emitted in
  attempts and validation. The delta does not touch any of these paths.
- **Pass 07 (2 items) — both remain RESOLVED.** `canary_diagnostics.py` / `gap_diagnostic.py`
  absent; frozen closure lists only the two declared `study:implementation/*.py`; R9 passes.
  Phase D.0 authority artifacts present and internally consistent.
- **Pass 08 — CLEAR, 4 notes.** Notes carried forward unchanged (pre-seal; causal lineage
  currency; caller-supplied `candidates_path`/`observations_path` not SHA-pinned to a preserved
  Phase C authority; no in-env `resolve_execution_manifest` re-run). The new fixture layout now
  makes the frame-source swap explicit but the paths are still caller-supplied and unpinned —
  defence in depth is the row-count + identity-order equality (`:123-126`), the
  `PHASE_D_DIRECTION_COLUMN_AMBIGUOUS` guard, and the TRAIN-year guard.
- **Passes 01–05 — CLEAR, nothing to re-adjudicate.**

## Notes (non-blocking)

1. `artifacts/preexec_audit_seal.json` binds the prior composite `4f45256b…` — expected
   pre-seal; the orchestrator re-seals once both pass-09 audits are CLEAR.
2. `artifacts/phase0_source_manifest.json` changed (`4b533719…` → `1fd86b8a…`). It is a
   generated provenance manifest inside the execution closure, regenerated by PREPARE to
   reflect the new composite; not a hand-authored contract and not part of the declared code
   delta. `frozen_execution_manifest.json` self-consistently binds the new value and
   `phase0_source_manifest_sha256` matches.
3. Independent CLI re-execution of `scripts/resolve_execution_manifest.py` and recomputation
   of file SHAs were not possible (no shell in this audit environment). Currency of
   `1a6eed85…` rests on the preflight `EXECUTION_MANIFEST` re-resolution (PASSED) and the
   readiness R8 double-resolution with exact equality.
4. `data/catalog/NQ_v0_2020_2026` is an uncommitted, gitignored junction — NOTE only,
   consistent with R1 (`DATASET_IDENTITY_OK`, catalog identity `20cd3365…` matches).

## Referred to lookahead-auditor

`load_phase_c_inputs` now sources the 13 causal features from the candidate surface and
`regime_direction` from the observation surface (`phase_d_modeling.py:134-141`); the
"favorable = regime direction" continuation-crosstab claim and the `direction = regime_direction`
(SIGNED_BY_DIRECTION) collector semantics are causal/semantic questions outside contract scope
— the concurrent causal pass 09 should confirm the frame-source swap introduces no
forward-outcome content and that the direction mapping is causally sound.

## Blocking verdict

**CLEAR.** The single-defect fix is confined to `load_phase_c_inputs` frame-source
resolution plus its matching fixture; a byte-level comparison of the frozen manifest against
the pass-08 audited-files map confirms `compiled_study.json`, `SPEC.md`,
`research_decision.yaml`, `study.yaml`, and every `config/*.json` are unchanged, so target
semantics, population, the 13-feature contract, folds, the 36-config grid, selection
priority, chronology, and the six cell definitions are structurally untouched. The fixed
driver now correctly reads features from candidates and `regime_direction` from observations,
asserts row-alignment on identity keys before the positional concat, adds a fail-closed
ambiguity guard, and the fixture now guards the layout that passes 06-08 missed. Preflight is
CLEAR (8/8) and readiness PASS (R1–R10) at composite `1a6eed85…`, which the preflight
re-resolution and readiness R8 double-resolution both confirm current. All pass-08 PASS rows
carry forward. Nothing blocks SEAL; the seal itself and the concurrent causal pass 09 remain
the orchestrator's to complete.
