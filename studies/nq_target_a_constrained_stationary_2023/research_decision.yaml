# Authoritative decision contract (research_decision.yaml > SPEC.md > study.yaml > compiled_study.json > code)
study_id: nq_target_a_constrained_stationary_2023
research_question: "Can a deliberately low-capacity LightGBM over only the stationary/relative T0 MTF + structural geometry of the audited Target-A surface (22 absolute-price columns removed) show genuine chronological Target-A information inside 2023 -- resisting shuffled labels, generalising 60%->20%, concentrating monotonically, and beating the exact MTF-state control? 2023 only; 2024/2025/2026 untouched."
method_note: "Methodology study over the REUSED audited 2023 T0 frame of nq_mtf_structural_predictive_ranking (merged controller output, sha-pinned; no replay). Bounded analysis script constrained_model.py; contract = artifacts/CAPACITY_LADDER.json + STATIONARY_FEATURE_SURFACE.json + TEMPORAL_SPLIT_2023.json, frozen by CONTRACT_FREEZE.json and committed before any fit."
status: DRAFT
dataset_id: NQ_1S_V2_GLOBEX
terminal_decisions: {}
# Predeclared fork policy (WORKFLOW.md §N.4). Agents follow these instead of asking again; a genuine
# semantic choice with no policy here is still a SEMANTIC_DECISION_REQUIRED question.
autonomy_decisions:
  on_capability_gap: stop_and_handoff                # write CAPABILITY_GAP_HANDOFF, end the session; never build it here
  platform_change_required: chore_branch_and_fresh_session
  deterministic_defect: auto_fix                     # fix, add a targeted test, re-run the bounded check
  calendar_reference_parity: common_interval_exact   # exact on the common calendar interval; Globex-only rows enumerated, descriptive
  frozen_parent_model: rescore_if_authenticated      # never retrain a frozen parent
  protected_period: never_expand_authority
