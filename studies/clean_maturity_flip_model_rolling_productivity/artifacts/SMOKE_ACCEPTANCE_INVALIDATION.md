# Invalidated smoke acceptance

The prior `smoke_acceptance.json` was issued from the rejected early-ETH-drop
experiment (1,698 candidates / 355 positives).  That experiment failed exact
parity against the accepted production path (1,704 / 361 / 1,343), so its
acceptance is retained as `smoke_acceptance_invalidated_1698.json` for audit
history only and is not a runtime acceptance artifact.

A fresh acceptance must be generated by the bounded governance lifecycle after
the current source closure is prepared, reviewed, sealed, and smoked.
