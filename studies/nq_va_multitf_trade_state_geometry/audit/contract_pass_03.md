# Contract audit — pass 03 (delta) — nq_va_multitf_trade_state_geometry

audit_type: "contract". Delta over `contract_pass_02` (CLEAR, 0 critical, 3 notes, K-1..K-6).
New composite `c9249f99ff96ab5b1dd8ae7f0876f490f6e1f9e3238fe4b7ae9cd71150748356`, plan
`11fcaf24a18e3c3f442e8a589a962f3cff6f075810fb136f14ac1b4720d1e557`. Packet tests 158/0.
Causal `pass_03`/`pass_04` (both CLEAR, C-6 closed, NOTE C-7) read and not duplicated.

| Requirement | Verdict | Code evidence | Test evidence | Smallest remediation |
|---|---|---|---|---|
| Research contract frozen (§1) | PASS | `study.yaml` content read in full: `outcome.session_end: truncate`, `session: TRADING_DAY`, 16 `barrier.arms` (ids/ATR levels), `chronology.train:[2023,2024]`/`prohibited:[2025,2026]`, `model: none` all match `research_decision.yaml` and the packet's `chronology`/`outcome.arms`/`columns` verbatim. `study.yaml` is not a member of any `stage_closures.*.files` list (only `research_workflow/host/outcomes.py`, `target_replay_oracle.py` + the new test file changed, per `stage_closures.outcome`/`collection`). Causal `pass_03` independently confirmed byte-identical against the pass_02-audited seal. | n/a | I could not run `git diff 14426fb1 HEAD -- study.yaml` myself this session (no shell tool available); relying on content cross-check + the causal auditor's independent diff. If a future audit has shell access, run the diff directly rather than re-deriving equivalence from content. |
| Stale v3 partitions cannot be reused (critical check) | PASS | `partition_reuse.declared.mode: off`, `effective: off`, `would_reuse: []`; both `train-2023`/`train-2024` carry `reusable: false`, `reason: REPLAY_CLOSURE_CHANGED:replay_closure_composite_sha256,replay_plan_sha256`, and `recorded_replay_closure_sha256` != `expected_replay_closure_sha256` for both years. The old partitions still carry the spent `manifest_seal e93c62bb…` / `manifest_plan_sha256 247755a8…`, distinguishable from this seal. | — | Two independent reasons hold (declared-off, and a hash mismatch that would still block reuse if mode were flipped to `on`), so re-enabling reuse alone cannot resurrect the censored v3 frame. None. |
| Observation column contract (93/15/64, `observed_seconds` last) | PASS (carried, unchanged) | Packet `columns.observation` unchanged in shape from pass_02 (93 total incl. 15 `terminal_*`, 64 `fp_*`, `observed_seconds` last); `stage_closures.outcome`/`collection` show only kernel logic (`outcomes.py`, `target_replay_oracle.py`) changed, never `research_workflow/grammar/compiler.py` (the column-contract surface) or `research_workflow/output_manager.py`'s merge-schema assertion. | 158/0 | None. |
| Terminal-label reachability | PASS | Pre-repair, every arm/terminal was structurally CENSORED at setup (24h horizon > ~23h TRADING_DAY, `kernel: composite` had no `truncate` path). Post-repair (`4139ae15`+`e341d709`), `effective_end = min(horizon_end, session_close)` makes POSITIVE/NEGATIVE/`SESSION_END`(now a real "observed-to-close" outcome, not "voided") all reachable; the flip terminal no longer depends on the row-level composite aggregate (`composite_kernel_discards_the_flip_terminal: FIXED_AND_REGRESSION_TESTED`). Causal `pass_04` independently traced every terminal path (incl. the close-instant 1s-before-1m race) to a resolved, non-null outcome or an explicit null with a named `censor_reason`/`exit_unavailable_reason` — never a silent gap. | 22 new unit cases (`test_composite_session_end_truncate.py`), `fixtures/first_passage_parity.json` (16 arms x independent oracle) | None. `terminal_net_pnl_points`/`_atr`/`terminal_cost_points` remain structurally always-null (no `outcome.cost_points_per_side` declared in `study.yaml`) — this is disclosed in `research_decision.yaml.residual_limitations.no_realized_terminal_pnl` and unchanged by this delta, not a fresh finding. |
| Zero study Python (R10) | PASS | Packet `study_python: {exception: null, python_files: []}`. | — | None. |
| Deliverables table / stage-closure scope coverage | PASS | The two changed files (`host/outcomes.py`, `target_replay_oracle.py`) are members of both `stage_closures.collection.files` and `stage_closures.outcome.files`; the `collection`/`merge` deliverables (`deliverables_by_stage.collection`/`merge`) are the artifacts this repair exists to make non-censored, and both closures cover the changed surface — no scope gap between the deliverable and the seal vouching for it. | — | None. |
| Disclosure/provenance sweep | WARNING (new) | See NOTE→WARNING K-7 below. | — | See below. |
| Test classification (158 narrow / 234 wide, 4 pre-existing) | NOT VERIFIED | `_work/controller/test_summary.json` records only aggregate `counts`, no node-id list, so I cannot confirm from artifacts alone whether the controller's 158-test gate actually includes `test_composite_session_end_truncate.py`'s 22 cases or only exercises a narrower slice that happens to also pass. Causal `pass_04`'s classification of the 4 wider failures as pre-existing/frozen-V1-path (identical with and without `e341d709`) is credible but is the causal auditor's own classification, not an artifact I independently re-derived. | 158/0 (controller); 234/4 (wider, per causal pass_04, not re-run by me) | Persist selected test node-ids (or the `test_selection` scope filter used) in `test_summary.json` so a contract audit can confirm the outcome-kernel diff is inside the gated scope without re-running tests. |

## Findings

### WARNING K-7 — `chore/composite_truncate`'s unmerged-to-`main` status is not recorded in `research_decision.yaml`
`research_decision.yaml` was updated for this delta (`collection_v3_gate_failure`, `lifecycle_outcome_policy_v3`) but names only branch `chore/fill_outcome_session` (C1, commits `7c4a2673`/`292c8ca4`) as unmerged to `main`. The two commits that actually fix this seal's blocking defect — `4139ae15` and `e341d709` — live on a *different* branch, `chore/composite_truncate`, named only in the causal auditor's own reports (`pass_03.md:11`, `pass_04.md:73`), never in the decision contract itself. `WORKFLOW.md §M.3` step 5 permits building on an unmerged platform change only "if the platform change is not yet on main and that is recorded" — grep confirms `composite_truncate`/`4139ae15`/`e341d709`/`8c3ad94d` appear nowhere in `research_decision.yaml`. This is the same disclosure category as pass_02's NOTE K-5 (C1's unmerged lineage); CLAUDE.md's rule is to sweep a category, not re-report it once per branch. Raising to WARNING (not CRITICAL) because the fact *is* disclosed, just in the wrong document — the audit trail, not the decision contract.
**Smallest remediation:** add one line to `research_decision.yaml.platform_notes` naming `chore/composite_truncate` (commits `4139ae15`, `e341d709`, merge `8c3ad94d`), and confirm it is not on `main` and its basis (`292c8ca4`/`chore/fill_outcome_session`).

## Referred to lookahead-auditor
None — nothing beyond what causal `pass_03`/`pass_04` already own.

## Carried forward from contract_pass_02
K-4 (year-role split is a contract obligation, not a runtime gate) and K-6 (superseded v2 evidence retained with correct disposition) — unchanged, this delta touches neither chronology nor the v2 partition. K-5 is superseded by K-7 above (same category, now covering both unmerged chore lineages). K-1 (claim stages have no declarations; study sealed/run only through merge) still applies — no collection/merge artifacts exist yet under this composite.

## Verdict

**CLEAR.** Zero critical, one warning (K-7, a provenance-location gap, not a technical defect — the fact itself is disclosed, just not in the authoritative decision file), one requirement recorded as NOT VERIFIED (test-scope-coverage artifact granularity) which does not block: the controller's own gate is green and the causal auditor's classification of the wider failures is corroborated by both being confined to the frozen, unrelated V1 `target_runtime` path. Terminal-label reachability — the reason this re-seal exists — is confirmed genuinely repaired, and the stale, 100%-censored v3 partitions are mechanically blocked from reuse by two independent reasons.

<!-- AUDIT_SUMMARY_V2_START -->
{"audit_type": "contract", "audited_execution_composite_sha256": "c9249f99ff96ab5b1dd8ae7f0876f490f6e1f9e3238fe4b7ae9cd71150748356", "auditor": "claude-contract-pass03", "critical": 0, "note": 0, "study": "nq_va_multitf_trade_state_geometry", "verdict": "CLEAR", "warning": 1}
<!-- AUDIT_SUMMARY_V2_END -->
