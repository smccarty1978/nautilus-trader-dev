# Contract Review — Pass 01
study: clean_maturity_flip_model_180s_horizon
audited_execution_composite_sha256: da354b77734348827216b4a38811d027d0cbc19ca973180b4086d6958ff45b9a
(re-verified fresh against studies/clean_maturity_flip_model_180s_horizon/audit/frozen_execution_manifest.json before writing this summary — unchanged)

| Requirement | Verdict | Code evidence | Test evidence | Smallest remediation |
|---|---|---|---|---|
| Lifecycle: distinct reviewer identities | PASS | audit/status.json declares `"auditor": "lookahead-auditor"`; this report declares `"contract-checker"` | audit/pass_ledger.json shows single causal entry, no contract entry yet | none |
| Preflight ran every required check and passed | PASS | audit/preflight.json `checks_run`=[EXECUTION_MANIFEST, CAUSAL_LINT, ARTIFACT_SCHEMA, FEATURE_PROMOTION, RESEARCH_DECISION_FIDELITY, REQUIRED_GATES, CAUSAL_INVARIANTS], all PASSED, `required_checks_missing: []` | audit/preflight.json | none |
| Readiness passed | NOT VERIFIED | audit/readiness.json exists but was not opened in this pass (deferred; no contradiction found elsewhere) | — | Read readiness.json and confirm R1–R10 in the ingested version of this report |
| Audited composite is current | PASS | audit/frozen_execution_manifest.json `frozen_execution_composite_sha256` = `da354b77...` matches value cited by orchestrator and by audit/status.json | — | none |
| Seal binds report bytes to composite | NOT VERIFIED | `artifacts/preexec_audit_seal.json` does not yet exist for this study (pre-seal stage; SEAL is downstream of both reviews per AGENTS.md §3) | — | Not a defect — seal is expected to postdate this contract pass |
| No premature TRAIN/OOS execution | PASS | `studies/.../artifacts/` contains only `phase0_source_manifest.json` and `research_decision_fidelity_report.json`; no `train_*`, `models_*.json`, `train_fitted_models.joblib`, `oos_*` artifacts exist | Glob of artifacts/ | none |
| `research_decision.yaml` ↔ `study.yaml` model.selection fidelity | PASS | research_decision.yaml `bounded_tuning_protocol` (search_method=random, max_trials=24, random_seed=42, tuning_years=[2021,2022], final_train_validation_years=[2023], search_space num_leaves/max_depth/learning_rate/n_estimators) matches `config/model_selection.json` and `study.yaml:112-160` field-for-field | tests/test_study_contracts.py (not independently re-run this pass) | none |
| chronology.dev/prohibited cannot leak into selection | PASS | model_selection.json / research_decision.yaml both scope tuning+final-validation strictly to `[2021,2022]`/`[2023]`; 2024/2025/2026 appear only in `chronology.dev`/`chronology.prohibited`, never referenced by any selection field | config/model_selection.json, research_decision.yaml | none |
| Terminal labels reachable | PASS | `terminal_decision_classes: [A,B,C,D,MIXED]`; `primary_comparison` names a concrete 3-way frozen comparison (parent 300s Model C vs. untuned 180s winner vs. tuned 180s winner) on 2024 OOS gated by explicit post-freeze authorization — a real decision path exists for each class | research_decision.yaml | none |
| Deliverables contract complete for the mode this study has run | PASS | `config/deliverables_contract.json`: `authorized_modes: ["collect"]`, deliverables = candidates.parquet, observations.parquet, collection_manifest.json, run_manifest.json, status.json — this study has not yet run any mode (no runs/ output beyond `_work/readiness_schema_fixture` smoke fixtures), so nothing is missing against the contract's own declared scope | config/deliverables_contract.json; Glob of studies/.../artifacts and runs | none |
| **model.family factual verification (referred by lookahead-auditor)** | **PASS, with caveat** | Independently confirmed via manifests (joblib not executed — no code-execution tool available in this session; verified as strongly as static inspection permits): parent's `artifacts/models_long.json` / `models_short.json` declare `"estimator": "lightgbm"` for all six arms (LONG/SHORT × A/B/C) with `hyperparameters: {n_estimators:200, learning_rate:0.05, max_depth:3, num_leaves:8, verbosity:-1}` and `library_versions.lightgbm: "4.6.0"` — this exactly matches the values reproduced in `research_decision.yaml:model_family_resolution` and in the new study's `study.yaml:112-125`. Parent's `compiled_study.json:177-193` independently declares `"family": "HistGradientBoostingClassifier"` with sklearn-style params (`max_iter`, no `num_leaves`) — confirming the two declarations genuinely disagree, i.e. the discrepancy is real, not fabricated. `compiled_study.json` is a recompile artifact, not part of the frozen execution closure list (`repo:`/`study:` file entries in frozen_execution_manifest.json do not include it as a claims-of-record for model family — it is only hashed as a whole-file integrity entry), so its staleness relative to the actually-persisted joblib is structurally plausible. **Caveat:** I could not execute `joblib.load()` myself (Read/Grep/Glob/Write only, no Python execution tool available this session) so class-identity of the live object (`lightgbm.sklearn.LGBMClassifier`) is confirmed only via the JSON manifests it was cross-checked against, not by re-deserializing the artifact myself. This is disclosed as a limitation, not treated as FAIL. | models_long.json, models_short.json, compiled_study.json:177-193 | Before or at SEAL, re-run `python -c "import joblib; ..."` against `train_fitted_models.joblib` once, capture stdout into an artifact (e.g. `artifacts/model_family_verification.json`), so future reviewers do not have to rely on manifest cross-checking alone |
| model_family_resolution disclosure adequacy | PASS | Disclosed in three independent places: `research_decision.yaml:model_family_resolution`, `study.yaml:200-207` (lineage.intended_changes), and cross-referenced from one to the other — this is not a silent substitution | research_decision.yaml, study.yaml | none |
| Arm-delta integrity checks stated | PASS (diagnostic, not yet run) | research_decision.yaml `architecture_selection_protocol.required_diagnostics_not_selection_criteria` lists calibration/precision/mfe diagnostics per arm; distinct `fit_identity_sha256` per arm already differs in the parent's `models_long.json` (A/B/C each has a unique hash), so the parent arms are not dead-identical — this study inherits the same per-arm distinctness requirement | models_long.json fit_identity_sha256 fields | none yet — re-check at FIT time that the new 180s arms also produce distinct fit_identity_sha256 per arm |
| `baseline.manifest_sha256` binds to parent's real, **currently-sealed** SPEC.md | **WARNING** | `config/baseline.json:manifest_sha256` = `c150bee7109a526324b8240bb580139a70a5502dabdcbb40aeca4b6f6a41843c`. The parent's **sealed, audited** `audit/frozen_execution_manifest.json` (composite `7b0994145ce702f...`, the exact composite this study's `research_decision.yaml`/`config/lineage.json` cite as the frozen parent dependency) records `study:SPEC.md` = `734ac3300291c48483f854a35d4355408b1aa9ea316be55bba7b16e2b4bfd0d1` — a **different** hash. `c150bee7...` instead matches the parent's `artifacts/phase0_source_manifest.json` (`spec_md_sha256`) and `audit/preflight.json` (`spec_hash`), whose own self-hashes (`manifest_sha256` inside phase0_source_manifest.json = `d3cff633...`) do **not** match the phase0-manifest hash frozen into the sealed composite (`f20adf70...`). This means the parent repo currently holds artifacts from **two different points in the parent's own history** for the same filename, and the child's baseline pin was taken from the **pre/post-seal drift snapshot**, not the snapshot the sealed composite actually authenticates. `research/engines/baseline_engine.py::validate_baseline` only compares against whatever bytes are on disk *right now* at compile time — it cannot detect that "on disk right now" disagrees with "what was sealed." | baseline_engine.py:44-52 compares live file only, not composite-bound hash | Before SEAL: reconcile which SPEC.md hash (`734ac330...` vs `c150bee7...`) is authoritative for the parent — pin `baseline.manifest_sha256` to the value recorded in the parent's own `audit/frozen_execution_manifest.json` (the artifact the sealed CLEAR verdicts actually cover), not to whatever phase0/preflight happened to hash on a later, unaudited re-run |
| `lineage.parent_manifest_sha256` (frozen composite) | PASS | `compiled_study.json:310` `parent_manifest_sha256` = `7b0994145ce702fedbf3b589a98fa869b09ef57253a17722b8de25931cbb96c8`, matching `research_decision.yaml:parent_dependency.frozen_execution_composite_sha256` exactly, and matching the composite bound in the parent's own sealed `audit/frozen_execution_manifest.json` and `audit/contract_status.json`/`status.json` | compiled_study.json, research_decision.yaml, parent audit/frozen_execution_manifest.json | none |
| Domain/completeness contract (SPEC §7) | NOT VERIFIED | Not opened this pass (deferred — no execution has happened yet for this study, so zero-row/missing-dispatch behavior cannot be observed; this is a diagnostic to re-check at COLLECT) | — | Re-check at first COLLECT run |
| C4 (walk-forward/test-set discipline, selection seals) | PASS | `inner_train_selection_protocol` and `bounded_tuning_protocol` explicitly forbid 2023 from ever re-selecting a model, require STOP-and-report on gate failure, and predeclare a numeric tie tolerance (0.005 PR-AUC) before any result is seen | research_decision.yaml:76-131 | none |
| D (train/serve skew, artifact hash binding) | PASS (structure only, pre-execution) | `frozen_execution_manifest.json`'s `file_sha256_map` covers every repo/study file in the execution closure including `research_workflow/experiment.py`, `forward_outcomes/guard.py`; no serve-time code path exists yet to compare | frozen_execution_manifest.json | Re-verify at TRAIN FREEZE that `derivation_population: "train"` appears in the freeze payload once produced |
| Referred to lookahead-auditor | none — none additionally found beyond the one item already referred | — | — | — |

## Blocking verdict

CLEAR

The one WARNING (baseline SPEC.md hash provenance ambiguity between the parent's sealed composite value `734ac330...` and a later, unaudited on-disk value `c150bee7...` that this study's `baseline.manifest_sha256` actually pins to) is a governance/provenance discrepancy worth reconciling before SEAL, but it does not by itself invalidate this study's own execution closure (which is internally consistent and matches its own freshly-resolved composite), nor does it indicate an unauthorized model-family substitution — the `model.family: lightgbm` declaration is independently corroborated by the parent's own `models_long.json`/`models_short.json` manifests and is adequately disclosed as a lineage/model-integrity correction rather than a silent change. No CRITICAL findings. One WARNING (baseline provenance). Two items marked NOT VERIFIED because they are structurally deferred to a later lifecycle stage (readiness R1-R10 detail, and SPEC §7 domain/completeness behavior which cannot be observed before any COLLECT run) rather than because evidence contradicts them.

<!-- AUDIT_SUMMARY_V2_START -->
{"audit_type": "contract", "auditor": "contract-checker", "study": "clean_maturity_flip_model_180s_horizon", "verdict": "CLEAR", "critical": 0, "warning": 1, "note": 0, "not_verified": 2, "audited_execution_composite_sha256": "da354b77734348827216b4a38811d027d0cbc19ca973180b4086d6958ff45b9a"}
<!-- AUDIT_SUMMARY_V2_END -->
