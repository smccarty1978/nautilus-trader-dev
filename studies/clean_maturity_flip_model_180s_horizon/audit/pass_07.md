# Look-Ahead & Timestamp Audit — Pass 07

**Date** 2026-08-27 · **Scope** NEW `implementation/two_phase_selection.py` and `tests/test_two_phase_selection.py` (full read, traced line-by-line against `research_workflow/model_selection.py` and `research/analysis/modeling.py::_build_estimator`), `study.yaml:model.selection` (random_state removal), `research_decision.yaml:architecture_selection_protocol` (updated), `audit/{preflight,readiness,lint,frozen_execution_manifest}.json` · **Scope hash (frozen execution composite)** `fe0b897360dab105b07d5cdd9e88ce8a2212a25fc0de487d38b354579a2fd0fa` · **Lint** 0 critical / 0 warning (`audit/lint.json`, 99/99 files, 100% coverage) · **Verdict** `CLEAR`

## Summary
Critical: 0 · Warning: 0 · Note: 1

## Prior findings adjudicated
| # | Finding | Status | Evidence |
|---|---|---|---|
| 1 | pass 06 — Phase-1 hyperparameter fidelity | WITHDRAWN (not re-raised — now executable, see below) | `two_phase_selection.py:53-59` `PARENT_FIXED_HYPERPARAMETERS` carries all 5 non-seed values |
| 2 | pass 03-06 referrals (parent SPEC.md drift, `model_family_resolution` joblib claim, `compiler.py` mirror-staleness) | WITHDRAWN (not re-raised; untouched this pass) | unchanged |

## (1) Composite freshness
`fe0b897360dab105b07d5cdd9e88ce8a2212a25fc0de487d38b354579a2fd0fa` matches exactly across `audit/frozen_execution_manifest.json`, `audit/preflight.json` (`status: CLEAR`), and `audit/readiness.json` (`prepared_execution_identity`, `overall_status: PASS`). `audit/lint.json` moved from 98→99 files scanned (the one new file the resolver's `resolve_study_files` actually globs — see §4).

## (2) Traced `two_phase_selection.py` against the four required assertions — all hold, in code
- **2023 unreachable in Phase 1** — `phase1_selection_spec()` (line 108) sets `final_train_validation_years=None`. In `run_model_selection` (`model_selection.py:382`), `_evaluate_final_validation` — the only function that reads `_selection_role=="final_validation"` rows — is gated by `if spec.final_train_validation_years`, so it is never invoked. Defense-in-depth: `run_phase1_architecture_selection` calls `_assert_no_years_outside(meta, TUNING_YEARS, phase="PHASE1")` (line 143) **before** either governed call, raising `TwoPhaseSelectionError` if any row's `_year` falls outside `{2021, 2022}` — verified by `test_phase1_rejects_2023_rows_present`, which asserts the raise happens with a 2023 row present. Two independent layers, both real, both exercised by tests.
- **Phase 2 receives exactly the Phase-1 winner** — `run_phase2_tuning_and_phase3_final_validation`'s signature takes `X_winner: pd.DataFrame` (singular), not a mapping (line 211); it constructs `{winning_arm: X_winner}` internally before calling `run_model_selection` (line 235), and `run_direction_two_phase_selection` (line 291-306) passes `X_final_by_arm[winning_arm]` where `winning_arm = phase1.winning_arm` — structurally there is no code path for a different arm to enter Phase 2. A post-call assertion (`if set(manifest["winner"].keys()) != {winning_arm}: raise`, line 237) additionally verifies the governed call itself only produced the one requested arm.
- **Final validation cannot re-enter selection** — same call-graph property verified in pass 05/06 (`winner = tied[0]` fixed from `scored`, itself built only from the tuning-fold `_fit_and_score` calls, before `_evaluate_final_validation` is ever invoked on `winner`). `run_phase2_tuning_and_phase3_final_validation` additionally asserts `selection_spec.final_train_validation_years == [2023]` exactly (line 228) — a caller cannot silently disable the gate for Phase 2/3 the way Phase 1 deliberately does. `test_phase2_rejects_final_validation_years_mismatch` exercises this.
- **A FAIL returns without fallback** — `run_direction_two_phase_selection` (line 308-316): on `phase23.final_validation_status != "PASS"`, returns a `DirectionResult(status="FAIL_DIRECTION", ...)` immediately. There is no loop, no re-invocation of `run_phase1_architecture_selection` or `run_phase2_tuning_and_phase3_final_validation` after that point in the function body — the function simply ends. `test_direction_fails_gate_and_stops_with_no_fallback` confirms `status == "FAIL_DIRECTION"` and the summary string contains `"STOP"`.
- **LONG/SHORT independence** — `run_study_two_phase_selection` (line 336-339) calls `run_direction_two_phase_selection` for `"LONG"` then `"SHORT"` as two ordinary dict-literal value expressions — Python evaluates these strictly left-to-right and sequentially (no concurrency), so LONG's call — including its manifest rename (`default_path.replace(out_path)`, line 246-247, using a **direction-specific** `output_manifest_name`) — fully completes before SHORT's call begins; the two directions cannot race on the shared default `artifacts/model_selection_manifest.json` path. `test_run_study_reports_both_directions_independently_never_pooled` asserts distinct manifest paths and that LONG's forced-impossible gate bound (`0.999`) never appears in SHORT's failure reasons.

One descriptive-only detail, not a defect: `manifest["no_retuning_after_final_validation_assertion"] = True` (`model_selection.py:416`) is a **hardcoded** field, always `True`, asserted by `test_phase2_produces_exactly_one_winner_and_gates_against_2023` — it documents the call-graph guarantee already verified above rather than computing/re-verifying it at runtime. Not misleading (the guarantee it names is real, per the trace above), just worth knowing it isn't itself a check.

## (3) The `random_state` fix — correctly resolves the crash, and does not reintroduce anything
Confirmed the mechanism directly: `research/analysis/modeling.py:209` (`_build_estimator`) always calls `LGBMClassifier(random_state=seed, **params)`. If `params` (the `hyperparameters` dict) also carries a `random_state` key, Python raises `TypeError: got multiple values for keyword argument 'random_state'` — this is real Python calling-convention behavior, not a hypothetical. `study.yaml:126-140`'s `model.selection.allowed_families[0].fixed_hyperparameters` now reads `{verbosity: -1}` only (confirmed in `config/model_selection.json` and `compiled_study.json`, both re-diffed this pass); `random_seed: 42` (a sibling field, not part of `fixed_hyperparameters`) still flows through `run_model_selection` → `_fit_and_score`/`_evaluate_final_validation` as the separate `seed=` argument (`model_selection.py:257-259, 279`) — the only place `random_state` is supplied. `two_phase_selection.PARENT_FIXED_HYPERPARAMETERS` (line 53-59) is consistent with this convention (`random_state` correctly absent; `PARENT_RANDOM_SEED = 42` supplied separately via `ModelSelectionSpec.random_seed`). No other `fixed_hyperparameters` object in the closure was touched, so no new duplicate-kwarg site was introduced. See Note for one un-remediated sibling copy of the same original bug.

## (4) Anything else — one discovery outside my scope, flagged rather than adjudicated here
While confirming file-level composite consistency, I found the new `implementation/two_phase_selection.py` is **not** part of `resolved_execution_file_list`/`frozen_execution_manifest.json` — only `tests/test_two_phase_selection.py` is (`scripts/resolve_execution_manifest.py::resolve_study_files` globs `tests/*.py` but has no equivalent handling for `implementation/*.py`, and the AST runtime/contract/governance closures never reach it because it is not yet imported from any real entrypoint — only from the test file via a manual `sys.path.insert`, which the AST resolver cannot trace to a physical path). This is manifest-completeness / seal-integrity territory (`docs/CAUSAL_CHECKLIST.md`'s scope table explicitly assigns "incomplete manifests" to contract-checker, not to me), so I am not adjudicating it as a causal finding here — see the referral below. I flag it because it is exactly the kind of gap that would let a future edit to this pass's verified guarantees go undetected by the freeze/re-audit machinery; contract-checker should treat it with urgency.

## Critical findings
None.

## Warnings
None.

## Notes
### [NOTE] `study.yaml:119-125` (`model.params`) still embeds `random_state: 42` alongside the same 5 hyperparameters just fixed elsewhere
This is the identical bug pattern this pass's `random_state` fix just removed from `model.selection.allowed_families[0]` and kept out of `two_phase_selection.PARENT_FIXED_HYPERPARAMETERS` — but `ModelSpec.params` (a separate, sibling field; `research/schemas/study_spec.py:445-447`) was not touched and still declares all 6 values including `random_state`. Repo-wide search found **no current code path** that forwards `ModelSpec.params` into `fit_model(hyperparameters=...)` (it is inert declarative metadata today), so this does not manifest as a live defect — but if any future code reaches for `study.yaml`'s `model.params` as a fixed-hyperparameter source (a plausible thing for someone to do, since it is the field literally named for this purpose) instead of `two_phase_selection.PARENT_FIXED_HYPERPARAMETERS`, it will hit the exact `TypeError` this pass just fixed twice. Model-integrity-declaration territory, not causal — referred below, not blocking.

## Referred to contract-checker
- `implementation/two_phase_selection.py` is absent from `frozen_execution_manifest.json`'s resolved file list — the module implementing the TRAIN-only chronological-split guarantees this and prior passes verified can be edited post-freeze with no composite change and no stale-freeze detection. Urgent: seal/manifest-completeness territory.
- `model.params` (`study.yaml:119-125`) still carries `random_state` alongside the same hyperparameters whose duplicate-`random_state` bug was just fixed in `model.selection.allowed_families` and in `two_phase_selection.PARENT_FIXED_HYPERPARAMETERS` — dormant only because nothing currently consumes it that way.
- (carried forward, unresolved) `discovered_parent_spec_md_drift`, `model_family_resolution` joblib-claim verification, `compiler.py` config/*.json mirror-staleness (repo-wide).

## Clean checks
A1–A5, B1–B7/B9/B10, C1, **C3 (re-verified against the now-executable orchestration code and its 9 passing synthetic-fixture tests — all four required chronological-isolation properties hold structurally)**, F1–F4, G1–G4, H1–H4 clean.

<!-- AUDIT_SUMMARY_V2_START -->
{"audit_type": "causal", "auditor": "lookahead-auditor", "study": "clean_maturity_flip_model_180s_horizon", "verdict": "CLEAR", "critical": 0, "warning": 0, "note": 1, "audited_execution_composite_sha256": "fe0b897360dab105b07d5cdd9e88ce8a2212a25fc0de487d38b354579a2fd0fa"}
<!-- AUDIT_SUMMARY_V2_END -->
